{
  "name": "task-automation-dashboard",
  "private": true,
  "scripts": {
    "dev": "chmod +x scripts/start-database.sh scripts/start-redis.sh && ./scripts/start-database.sh && ./scripts/start-redis.sh && pnpm --parallel -r run dev"
  }
}
